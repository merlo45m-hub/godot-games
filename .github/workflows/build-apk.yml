name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  export-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download Godot
        run: |
          wget -qO godot.zip https://github.com/godotengine/godot/releases/download/4.2-stable/Godot_v4.2-stable_linux.x86_64.zip
          unzip -qo godot.zip
          chmod +x Godot_v4.2-stable_linux.x86_64

      - name: Export Templates
        run: |
          mkdir -p $HOME/.godot/export_templates/4.2-stable
          wget -qO templates.tpz https://github.com/godotengine/godot/releases/download/4.2-stable/Godot_v4.2-stable_export_templates.tpz
          unzip -qo templates.tpz
          mv templates/* $HOME/.godot/export_templates/4.2-stable/

      - uses: android-actions/setup-android@v3

      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - run: yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true

      - name: Export APK
        run: |
          mkdir -p builds
          ./Godot_v4.2-stable_linux.x86_64 --headless --export-debug "Android" builds/agent007.apk
        env:
          ANDROID_HOME: ${{ env.ANDROID_HOME }}

      - uses: actions/upload-artifact@v4
        with:
          name: agent007-apk
          path: builds/*.apk